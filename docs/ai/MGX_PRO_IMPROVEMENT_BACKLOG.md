# MGX Pro Improvement Backlog

| ID | Variant | Issue | Metric Evidence | Priority | Suggested Fix |
| -- | ------- | ----- | --------------- | -------- | ------------- |
| P4Y-001 | D02 | Step4-Y fresh corpus keeps only one high-confidence manual-fix bucket alive: `strongA5 second-pressure`. | Dataset validation returns `71` valid rows, `0` invalid rows, and `trainingAllowed=true`; fresh counterfactual keeps `D02 strongA5 second-pressure` at `sampleCount 57`, `meanDelta -232.98`, `confidence 1.0`, verdict `STABLE_STANDARD_BETTER`. | P0 | If Pro heuristics continue at all, keep them strictly inside `D02 strongA5 second-pressure`. Everything else should wait for larger replay corpora or dataset-driven work. |
| P4Y-002 | S01/S02 | Step4-Y fresh corpus does not yield a stable replay-backed S01 or S02 good-hand bucket, so further manual heuristics are low-confidence. | Fresh corpus comparison keeps `S01 strongSD27 top-end pressure`, `S02 premiumSDA5 CALL/RAISE`, and `S02 strongSDA5 CALL/FOLD/RAISE` in `NOISY` / `NEEDS_MORE_SAMPLES`, and exporter excludes them from the `71` training rows. | P0 | Do not add new S01/S02 heuristics from intuition. Expand replay samples or move these buckets directly into Iron/WorldMaster action-value supervision. |
| P4X-001 | D02 | Step4-X safely restores the replay-backed `strongA5 second-pressure` exception, but the bucket delta and aggregate EV do not improve. | Counterfactual `reports/ai-eval/counterfactual-score-d02-s01.json` keeps `D02 strongA5 second-pressure` at `meanDelta -267.5`, while targeted `500x3` moves from `Gap -6.8` to `-7.89` and full-suite stays `-11.53`. | P0 | Do not widen D02 heuristics further from intuition. Refresh the divergence corpus after the live patch or move this bucket into action-value supervision. |
| P4X-002 | S01 | Step4-X safely restores top-end `strongSD27` pressure calls, but the gain is only visible in targeted aggregate and not in the required full-suite average. | Targeted `500x3` improves from `Gap -11.09` to `-10.17`, but full-suite stays `Pro 8.0 / Std 22.0 / Gap -14.0`; counterfactual stable bucket remains `meanDelta -57.91` on the historical corpus. | P0 | Keep the Step4-X S01 pressure retention, but do not add broader S01 medium-defense heuristics. Use fresh replay capture or action-value labels before another S01 pass. |
| P4W-001 | D02 | Step4-W finally turns `strongA5 second-pressure` into a stable replay-backed Standard-better bucket. | Fresh `step4w` corpus writes `9000` D02 samples and focused counterfactual scoring yields `sampleCount 40`, `meanDelta -267.5`, `positiveRate 0.10`, `stabilityAcrossSeeds 0.913`, verdict `STABLE_STANDARD_BETTER` for `Pro FOLD / Std RAISE`. | P0 | Step4-X should only widen D02 in this exact bucket: safe `strongA5` second-pressure retention. Keep `trashA5` frozen because the verify bucket is still noisy. |
| P4W-002 | S01 | Step4-W turns `strongSD27 top-end pressure` into a stable replay-backed Standard-better bucket. | Fresh corpus writes `9000` S01 samples and focused counterfactual scoring yields `sampleCount 51`, `meanDelta -57.91`, `positiveRate 0.20`, `stabilityAcrossSeeds 0.8261`, verdict `STABLE_STANDARD_BETTER` for `Pro FOLD / Std CALL`. | P0 | Step4-X can safely restore top-end `strongSD27` pressure calls in the narrow replay-backed bucket only. Do not reopen broad medium or trash S01 defense. |
| P4W-003 | S02 | Step4-W still cannot stabilize S02 good-hand replay buckets. | Fresh corpus writes `9000` S02 samples, but `strongSDA5 CALL/FOLD/RAISE` stays `NEEDS_MORE_SAMPLES` at `29` samples for `FOLD vs RAISE` and `NOISY` at `28` samples for `CALL vs RAISE`; `premiumSDA5 CALL/RAISE` is also `NOISY` at `25` samples. | P0 | Do not add another S02 heuristic pass yet. Expand the replay corpus or graduate to action-value dataset generation before touching S02 again. |
| P4W-004 | D02/S01/S02 | Weak/trash replay buckets remain too noisy for heuristic reopen. | `D02 trashA5 FOLD/CALL verify` reaches `492` samples but is still `NOISY`; `S01 trashSD27 FOLD/CALL verify` and S02 weak/trash verify buckets never become stable action targets. | P0 | Treat weak/trash replay rows as dataset candidates or verification-only evidence. Do not use them to reopen guards in rule-based Pro. |
| P4V-001 | S02 | Step4-V keeps the replay-backed `strongSDA5` pressure exception live without reopening weak/trash leaks, but the required aggregate still does not move. | Step4-V targeted avg stays at `Pro 8.96 / Standard 21.04 / Gap -12.09`; full-suite stays at `Pro 4.87 / Standard 25.13 / Gap -20.27`; `counterfactual-score-d02-s02-s01.json` still reports the original `Pro FOLD / Std RAISE` bucket because the replay corpus stores the historical divergent action pair. | P0 | Keep the Step4-N guard and the Step4-V strong-hand exception. Next S02 work should expand replay sample volume around premium/strong sparse good-hand spots instead of adding more heuristics. |
| P4V-002 | D02 | Step4-V keeps a narrow replay-backed `strongA5 second-pressure` exception live, but the full-suite D02 gap remains exactly outside the experimental threshold. | Step4-V targeted avg stays `Pro 11.60 / Standard 18.40 / Gap -6.80`, while full-suite stays `Pro 9.23 / Standard 20.77 / Gap -11.53`. Counterfactual bucket still reports `Delta -200` on the original divergent action pair. | P0 | Keep the narrow D02 strong-hand exception, but do not widen defense from heuristic intuition. Either collect more replay rows for this bucket or move attention back to S02/S01 where sparse good-hand spots remain. |
| P4V-003 | S01 | Step4-V safely keeps the top-end `strongSD27` pressure retention live, but replay evidence is still too sparse to justify another S01 expansion. | Step4-V targeted avg stays `Pro 9.46 / Standard 20.54 / Gap -11.09`; full-suite stays `Pro 8.00 / Standard 22.00 / Gap -14.00`; counterfactual bucket remains `Pro FOLD / Std CALL`, `Delta -33.33`, `Samples 3`. | P1 | Keep the Step4-P call guard and Step4-V top-end pressure retention intact. Expand only if replay volume increases and the bucket survives longer-sample re-score. |
| P4U-001 | D02 | Step4-U counterfactual replay shows the cleanest remaining replay-backed edge is top `strongA5` second-pressure retention, not another trash-defense rollback. | `counterfactual-score-s02-s01-d02.json` records `D02 strongA5 second-pressure` with `Pro FOLD / Std RAISE`, `ProEV 0`, `StdEV 200`, `Delta -200`, `Samples 3`, verdict `STANDARD_BETTER`. | P0 | Keep D02 trash defense frozen and add only a narrow replay-backed exception for top `strongA5` second-pressure spots. |
| P4U-002 | S02 | Step4-U replay confirms the next S02 candidate is a sparse `strongSDA5` good-hand pressure bucket, not another weak/trash defense change. | Counterfactual replay records `S02 strongSDA5 CALL/FOLD/RAISE` with `Pro FOLD / Std RAISE`, `ProEV 0`, `StdEV 140`, `Delta -140`, `Samples 3`, verdict `STANDARD_BETTER`, while `trashSDA5 FOLD/CALL verify` remains observationally risky despite a positive replay delta. | P0 | Keep the Step4-N guard intact. If S02 is revisited, add only a narrow replay-backed `strongSDA5` pressure retention bucket. |
| P4U-003 | S01 | Step4-U replay shows the remaining S01 issue is top-end pressure retention, while the old trash-call bucket should stay locked. | `S01 strongSD27 top-end pressure` comes back `Pro FOLD / Std CALL`, `Delta -33.33`, `Samples 3`, verdict `STANDARD_BETTER`; `trashSD27 FOLD/CALL verify` is only `Delta -1.33` with verdict `NO_CLEAR_EDGE`. | P1 | Keep the Step4-P call guard intact and revisit only replay-backed top-end `strongSD27` pressure retention. |
| P4U-004 | S01/S02/D02 | Step4-U proves observational divergence alone is no longer enough for safe heuristics; replay-backed selection is now required. | Replay report produces `27` samples, `54` valid replays, `0` invalid replays, and no `PRO_BETTER` bucket yet. | P0 | Use counterfactual replay before reopening any weak/trash divergence bucket, and expand the replay sample pool before making larger rule changes. |
| P4T-001 | S02 | Step4-T shows the remaining S02 gap is now mostly sparse good-hand under-capture, not another weak/trash defense failure. | Combined `300-hand x 3 seed` divergence mining records `5400` differing rows, but the high-frequency buckets are noisy `trashSDA5 FOLD vs standard CALL` spots. The clean heuristic candidates are sparse `premiumSDA5 CALL vs RAISE` and `strongSDA5 FOLD/CALL vs RAISE` spots in `3way` post-draw pressure. | P0 | Keep the weak/trash guard frozen. If S02 is revisited, touch only narrow premium/strong post-draw raise/value buckets that appear in the Step4-T divergence report. |
| P4T-002 | S01 | Step4-T shows S01 no longer needs another broad defense rewrite; the remaining clean candidate is sparse top-end `strongSD27` post-draw pressure retention. | Combined mining still has many noisy trash/weak `FOLD vs CALL` buckets, but the only top-end candidate is `strongSD27` overfold in rare `4way+` pressure spots. | P1 | Keep the Step4-P guard intact. If S01 is revisited, only test tiny top-end post-draw `CALL` retention with longer-sample validation first. |
| P4T-003 | D02 | Step4-T confirms D02 is no longer moving on tiny defense trims; the remaining high-frequency divergence is noisy trash multiway `FOLD vs CALL`, not a clean heuristic bucket. | Combined mining shows `trashA5` multiway `FOLD vs CALL` as the highest weighted D02 divergence, but the sign flips by seed and position, and Step4-S leaves the aggregate unchanged at `Gap -11.5`. | P1 | Do not reopen D02 trash defense from observational divergence alone. Revisit D02 only with longer-sample repeated-pressure traces or explicit replay tooling. |
| P4S-001 | D02 | Step4-S safely trims `strongA5` repeated-pressure defense and narrows `mediumA5` small-call eligibility, but the required full-suite average does not move. | Step4-S keeps full-suite D02 at `Pro 9.2 / Standard 20.8 / Gap -11.5 / Fallback 0.0000`, while focused `300-hand` only shifts from Step4-O `Pro 11.7 / Standard 18.3 / Gap -6.6` to `Pro 11.6 / Standard 18.4 / Gap -6.8`. Seed-level focused traces do reduce `callRate` and `losingCallRate` on some seeds, but the detailed `20260507` `100-hand` before/after trace is aggregate-identical. | P1 | Keep the tighter D02 defense in place, but stop expecting tiny `strongA5` / `mediumA5` trims to move the required suite. Either gather longer-sample D02 traces around repeated pressure or move attention back to S02/S01 value realization. |
| P4R-001 | S02 | Step4-R confirms the residual S02 issue is not an obvious missing good-hand line anymore; safe pre-draw/4way+ value expansion produced no measurable change in required samples. | Step4-R keeps focused avg at `Pro 8.96 / Standard 21.04 / Gap -12.09` and full-suite avg at `Pro 4.87 / Standard 25.13 / Gap -20.27`, effectively identical to Step4-Q while fallback stays `0.0000`. | P0 | Keep weak/trash guard frozen and stop nibbling at S02 with tiny heuristic tweaks. Either move to longer-sample validation for S02 variance, or prioritize D02/S01 where rule changes still move aggregate EV. |
| P4Q-001 | S02 | Step4-Q confirms the residual S02 gap is now a sparse good-hand value problem, not a weak/trash defense problem. | Full-suite avg improves slightly from Step4-P `Pro 4.7 / Std 25.3 / Gap -20.7` to Step4-Q `Pro 4.9 / Std 25.1 / Gap -20.3`, while focused `300-hand` avg improves from `Gap -13.0` to `Gap -12.1`; fallback remains `0.0000`. | P0 | Keep the weak/trash call guard frozen. Next work should target `premiumSDA5` pre-draw multiway continue and `strongSDA5` / `upperMediumSDA5` sparse `4way+` value spots only. |
| P4P-001 | S01 | Step4-P removes the inherited `CALL` leak and sharply compresses the S01 gap, so S01 is no longer the top blocker. | Full-suite avg improves from Step4-O `Pro -0.3 / Std 30.3 / Gap -30.5 / Fallback 0.2631` to Step4-P `Pro 8.0 / Std 22.0 / Gap -14.0 / Fallback 0.0000`; detailed `100-hand x 3 seed` run records `742` S01 call-guard hits (`351` trash, `367` weak, `24` lower-medium). | P1 | Keep the Step4-P call guard intact. If S01 is revisited, only add very small premium/strong value refinements. |
| P4O-001 | D01 | Step4-O removes most of the D01 gap and all D01 fallback, so D01 is no longer the highest-priority leak. | Full-suite avg improves from Step4-N `Pro -3.0 / Std 33.0 / Gap -36.0 / Fallback 0.1311` to Step4-O `Pro 13.9 / Std 16.1 / Gap -2.1 / Fallback 0.0000`; focused `300-hand` avg is now `Pro 17.8 / Std 12.2 / Gap +5.7`. | P1 | Keep the Step4-O late-defense trim intact. If D01 is revisited, only make small smooth `8-low` and top `strong27TD` value refinements. |
| P4N-001 | S02 | Step4-N fixes the worst early inherited-`CALL` leak, but S02 still trails Standard on full-suite average because the remaining gap has shifted away from trash/weak defense and toward residual value realization. | Step4-N full-suite avg Pro improves from Step4-M `-8.6` to `4.7`, Standard drops to `25.3`, gap improves from `-47.2` to `-20.7`, fallback goes to `0.0000`, and the detailed `100-hand x 3 seed` run records `745` call-guard hits (`689` trash, `50` weak, `6` lower-medium). | P0 | Keep the Step4-N early guard intact. Do not trim premium/strong value again. Next isolate residual `upperMediumSDA5` multiway spots and late value realization instead of adding more weak-hand defense. |
| P4K-001 | S01 | Step4-K/Step4-J left a large S01 gap, but Step4-P shows the structural fix was inherited-`CALL` removal rather than more value tuning. | Step4-P suite moves S01 to `Pro 8.0 / Std 22.0 / Gap -14.0` with `Fallback 0.0000`, and focused `300-hand` gap to `-11.1`. | P2 | Do not reopen weak/medium defense. Only revisit S01 for very small top-end value adjustments after S02 and D02 are addressed. |
| P4L-001 | S02 | Step4-L improves S02 in focused `300`-hand runs, but the required full-suite `100`-hand average still regresses. | Step4-L focused avg Pro `-4.6` vs Standard `34.6` and gap `-39.2` (better than Step4-K `-44.0`), but full-suite avg Pro `-6.3` vs Standard `36.3` regresses from Step4-K `-5.4`. | P0 | Keep the new `upperMediumSDA5/lowerMediumSDA5` split and find the full-suite instability inside `premiumSDA5/strongSDA5` multiway value spots before changing defense again. |
| P4F-003 | D02 | Weak-hand defense is fixed, but a small `strongA5` repeated-defense bucket remains. | Step4-F 3-seed avg Pro `9.2` vs Standard `20.8`; 300-hand avg gap `-6.6`; remaining `strongA5` losses are concentrated in `large` pre-draw calls and `medium` second-street calls. | P1 | Keep `premiumA5` unchanged, but narrow the surviving `strongA5` open-pressure continue rule without losing the profitable value-bet line. |
| P4D-005 | D03 | Badugi is stable but still only ties Standard. | 3-seed avg Pro `7.5` vs Standard `7.5`; fallback `0.0000` | P1 | Improve medium made Badugi value betting and cheap-call defense to move above neutral. |
| P4D-006 | D02/S01/S02 | Pat thresholds remain the dominant draw-family leak class outside D01. | Step4-C attribution showed `PAT too weak` as the top EV loss bucket across the draw family, and Step4-O largely resolves the D01 share of that leak. | P2 | Refine pat/draw boundaries with separate logic for premium, medium, and trash finals, prioritizing S01/S02 after D02. |
| P4D-007 | B01/B05/B06/ST1/ST3 | Major 10 board/stud variants are still not in live Pro evaluation. | `NOT_RUN: NEEDS_PRO_RULES` | P1 | Implement executable Pro betting rules and evaluation-compatible routing for board/stud families. |
