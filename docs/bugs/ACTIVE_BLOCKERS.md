# Active Blockers

Last updated: 2026-05-21

This file contains release-impacting unresolved P0/P1 rows from `current_bugs.md`.
Rows that primarily need real-device proof are also listed in `PHYSICAL_QA_PENDING.md`; the active blocker entry explains why they still block friend alpha.

Physical blocker closure rule:
- Any active blocker whose primary owner is `PHYSICAL_QA_PENDING.md` requires a completed physical QA evidence packet before it can be closed or moved to verified monitor.
- Use `docs/testing/MGX_PHYSICAL_QA_EVIDENCE_FRAMEWORK.md`, `docs/testing/MGX_PHYSICAL_QA_RUNBOOK.md`, and `docs/testing/MGX_PHYSICAL_QA_EVIDENCE_TEMPLATE.md`.
- The packet must include device/browser/mode, release commit/hash, scenario evidence, screenshot paths, export/log paths, phase/actor/legal-action capture, player statuses, pass/fail result, residual risk, reviewer, and date.
- Do not close a physical blocker from local, desktop, or Playwright-only evidence.

Summary:
- Active blocker rows: 22
- Primary active-only rows: 6
- Duplicated from physical QA pending: 16
- Friend alpha status: HOLD

| ID | Severity | Area | Current failing behavior | Reproduction evidence | Blocking reason | Required next action |
|---|---:|---|---|---|---|---|
| `BADUGI-BET-DRAW-TRANSITION-001` | P0 | Badugi physical mobile tournament | Physical path previously reached a closed BET state that did not advance cleanly into DRAW. Automated/live emulation is fixed, but the original device path is not rechecked. | Physical mobile live observation; `badugiBetToDrawTransitionRegression.test.js`; `badugiTournamentBetToDrawSnapshotRegression.test.jsx`; `badugi-tournament-bet-to-draw-regression.spec.ts`; live Badugi tournament emulation PASS. | A physical recurrence blocks tournament progression. Primary owner: `PHYSICAL_QA_PENDING.md`. | Recheck on `https://mgx-poker.com/?mgxQa=mobile`; export snapshot if BET/DRAW stalls or diverges. |
| `BADUGI-DRAW1-CPU-ACTION-001` | P0 | Badugi live tournament DRAW action | DRAW1 CPU action failure is fixed in local/live emulation, but physical mobile confirmation is missing. | Prior live trace showed `controller action returned no snapshot`; focused unit/UI/E2E/live regressions PASS. | DRAW action failure can stall tournament hand progression. Primary owner: `PHYSICAL_QA_PENDING.md`. | Recheck Badugi tournament DRAW1 on physical mobile. |
| `BADUGI-HAND-SHAPE-001` | P0 | Badugi physical mobile tournament | Badugi physical path previously showed five-card hand contamination after draw-lowball paths. Automated/live checks now reject stale five-card snapshots, but device proof is missing. | Physical iPhone screenshot; Badugi hand-shape unit/UI/E2E PASS; cross-variant live regression PASS. | Cross-variant contamination corrupts game rules and can freeze tournament play. Primary owner: `PHYSICAL_QA_PENDING.md`. | Recheck cross-variant cash-to-Badugi tournament on physical iPhone. |
| `BADUGI-FOLD-DRAW-FREEZE-001` | P0 | Badugi physical mobile tournament | Folded Hero / folded seats in DRAW no longer reproduce in automation, but the original physical freeze path is not rechecked. | Physical iPhone screenshot; folded-draw actor unit/UI/E2E PASS; live Badugi tournament 20-hand portrait/landscape PASS. | Folded player revival or no-next-draw actor freezes are release blockers. Primary owner: `PHYSICAL_QA_PENDING.md`. | Recheck Hero-fold-in-DRAW tournament scenario on physical mobile. |
| `PHYSICAL-MOBILE-BADUGI-WAITING-001` | P0 | Badugi physical mobile tournament | Physical iPhone observed Badugi tournament waiting freeze, including recurrence after draw-lowball cash contamination. Fixes are deployed/emulation-verified, but physical result is unknown. | iPhone screenshots; live preview; build `a73042dcd2eca92097aa27a2d3f732b648fd49f4`; live cross-variant and Badugi tournament emulation PASS. | This is the umbrella physical Badugi tournament freeze blocker. Primary owner: `PHYSICAL_QA_PENDING.md`. | Run `docs/alpha/MGX_PHYSICAL_MOBILE_QA_RECHECK_STEPS.md`; export freeze JSON if reproduced. |
| `UI-MOBILE-TOURNAMENT-LANDSCAPE-001` | P0 | Mobile tournament landscape controls | iPhone Safari/PWA landscape can clip Hero Call/Raise/Fold because real visual viewport is smaller than CSS viewport. Local fix exists; physical proof is missing. | Physical iPhone Safari/PWA screenshots; `mobile-tournament-landscape-action-buttons.spec.ts`; `iphone-safari-tournament-landscape-controls.spec.ts`; `mobile-tournament-visual-viewport.spec.ts`. | Unusable controls on real mobile block friend alpha. Primary owner: `PHYSICAL_QA_PENDING.md`. | Deploy/recheck normal Safari and PWA landscape; prove action buttons visible/tappable without scroll. |
| `BADUGI-CASH-OPENING-ACTOR-001` | P0 | Badugi cash opening action | Physical iPhone Safari observed Badugi cash freeze at first opening actor. Local candidate and automated gates pass, but physical proof is missing. | Physical iPhone observation; `badugiCashOpeningActorRegression.test.js`; `badugiCashOpeningActorSnapshotRegression.test.jsx`; `badugi-cash-opening-actor-freeze.spec.ts`. | First-hand cash freeze blocks Core5 mobile alpha. Primary owner: `PHYSICAL_QA_PENDING.md`. | Recheck Badugi cash opening actor on physical iPhone Safari after deploy. |
| `BADUGI-DRAW-BET-MIX-001` | P0 | Badugi physical mobile phase sync | Physical screenshot showed DRAW/BET labels or controls diverging. Automated detector exists, but real-device recheck is pending. | Second physical mobile screenshot; `drawBetMixedStateRegression.test.jsx`; recheck steps in `MGX_PHYSICAL_MOBILE_QA_RECHECK_STEPS.md`. | Mixed betting/draw controls corrupt legal action visibility. Primary owner: `PHYSICAL_QA_PENDING.md`. | Recheck physical Badugi tournament; add focused export regression if reproduced. |
| `CROSS-VARIANT-STATE-001` | P0 | Core5 session/controller state | Physical Badugi tournament showed draw-lowball contamination after D01/D02/S01/S02 cash paths. Local/live fixes pass; physical proof is missing. | `MGX_CROSS_VARIANT_STATE_AUDIT.md`; `crossVariantStateResetRegression.test.jsx`; controller reset boundary tests; live physical-cross-variant regression PASS. | Variant contamination can corrupt hand shape, actor truth, and tournament state. Primary owner: `PHYSICAL_QA_PENDING.md`. | Recheck cross-variant mobile path on physical device. |
| `TOUR-SEAT-LIFECYCLE-001` | P0/P1 | Tournament table readability | Physical mobile Badugi tournament showed busted/out CPU panels staying as large seats and blocking table readability. Local display policy exists; deploy/physical proof is pending. | Physical mobile screenshot; `MGX_TOURNAMENT_SEAT_DISPLAY_POLICY.md`; rail display/eligibility tests; `tournament-busted-seat-readability.spec.ts`. | Busted seats must not block cards, pot, controls, or actor readability. Primary owner: `PHYSICAL_QA_PENDING.md`. | Deploy and physically confirm busted seats are compact rail-only and not actor/draw/bet eligible. |
| `UI-MOBILE-TABLE-DENSITY-001` | P1 | Mobile tournament table readability | Local candidate improves battlefield ratios, but real iPhone readability is still unconfirmed. | Physical tournament QA; `mobile-tournament-readability.spec.ts`; `mobile-battlefield-ratio.spec.ts`; `reports/ui/mobile-layout-policy/`. | Friend alpha needs readable Hero cards, active actor, pot, and controls on real phone. Primary owner: `PHYSICAL_QA_PENDING.md`. | Physical portrait/landscape tournament readability recheck. |
| `UI-MOBILE-HUD-OVERLAY-001` | P1 | Mobile tournament HUD density | HUD/rail/phase metadata previously competed with Hero cards/table center. Emulation passes; real-device overlap is unconfirmed. | Physical iPhone tournament QA; mobile layout/readability policies; `mobile-battlefield-ratio.spec.ts`. | HUD overlap can make tournament controls or cards unusable. Primary owner: `PHYSICAL_QA_PENDING.md`. | Physical recheck; reopen P0 if HUD covers Hero cards or actions. |
| `UI-MOBILE-LANDSCAPE-CONTROLS-001` | P1 | Mobile landscape controls | Explicit landscape layout passes automation, but physical Safari/PWA tap comfort is unconfirmed. | Physical iPhone tournament QA; `mobile-layout-mode-regression.spec.ts`; `mobile-battlefield-ratio.spec.ts`. | Landscape controls must be instantly tappable without scroll. Primary owner: `PHYSICAL_QA_PENDING.md`. | Physical iPhone Safari/PWA landscape recheck. |
| `UI-MOBILE-LAYOUT-MODE-001` | P1 | Mobile layout mode policy | Explicit portrait/landscape modes pass automation, but both orientations need physical validation. | `MGX_MOBILE_LAYOUT_POLICY.md`; `mobile-layout-mode-regression.spec.ts`; `mobile-layout-mode-regression.json`. | Incorrect layout mode can cause hidden controls or compressed table on real device. Primary owner: `PHYSICAL_QA_PENDING.md`. | Physical recheck both orientations before closing. |
| `D01-BLIND-POSTING-001` | P1 | D01 physical mobile blind display | Physical/live preview showed Hero labeled BB with `BET 0`, Pot 30, To Call 20. Local engine/UI traces pass, but physical proof is missing. | iPhone screenshot; D01 blind invariant/snapshot tests; `d01-blind-posting-regression.spec.ts`. | Blind/position mismatch confuses actor legality and pot state. Primary owner: `PHYSICAL_QA_PENDING.md`. | Deploy and physical recheck; reopen P0 if actual blind/pot/stack trace is broken. |
| `BG-005` | P1 | Mobile manual QA | Physical mobile QA remains active: automation/deployed smoke pass, but real-device Badugi tournament waiting and DRAW/BET divergence findings are unresolved. | `MGX_CORE5_PHYSICAL_MOBILE_QA_RESULT.md`; `MGX_FRIEND_ALPHA_GO_NO_GO.md`; physical screenshots tracked under concrete P0 rows. | Umbrella friend-alpha mobile QA blocker. Primary owner: `PHYSICAL_QA_PENDING.md`. | Resolve concrete physical P0s, then rerun iOS/Android QA. |
| `PREVIEW-DEPLOY-02` | P1 | Remote sync | Branch head is local-only and ahead of origin; push requires a credentialed environment. | `docs/deploy/MGX_ALPHA_REMOTE_SYNC_STATUS.md`; `git branch -vv` on 2026-05-19. | Friend alpha cannot rely on an unpushed local-only release head. | Push from credentialed environment without exposing tokens. |
| `UI-REPLAY-READABILITY-001` | P1 | Replay readability | Replay frame controls work, but rows are frame/raw-seat oriented and do not group actions by street/name/position/action order clearly enough. | `MGX_REPLAY_UX_REDESIGN_PROPOSAL.md`; replay screenshot; `ui-readability-smoke.spec.ts` replay row PASS. | User-visible replay understanding remains below alpha quality bar. | Build grouped replay view model and action order summary. |
| `CORE5-CPU-FOLD-001` | P1 | Core5 cash CPU strategy | Physical preview felt fold-heavy; local heuristic path is acceptable, but pro-overlay sanity mode is extremely nitty and live row source attribution is incomplete. | `MGX_CORE5_CPU_STRATEGY_SANITY_AUDIT.md`; `MGX_LIVE_CPU_ACTION_DB_AUDIT.md`; `live-db-cpu-action-audit-v2.json`; CPU metadata tests PASS. | Alpha gameplay quality depends on knowing the deployed decision source and fold rate. | Deploy explicit CPU decision metadata, run targeted cash session, audit by sessionId. |
| `BADUGI-CPU-VALUE-BET-001` | P1 | Badugi CPU value pressure | Adapter/schema fix is deployed and local value-pressure improves, but live proof has not captured a pro-overlay BET pressure row with canonical final raise/bet. | `MGX_BADUGI_VALUE_BET_AUDIT.md`; value-bet audit reports; live observation; normalization tests. | Badugi CPU may still appear passive in real alpha sessions. | Run targeted live/physical Badugi session until pressure-row telemetry is captured. |
| `CPU-TOO-NIT-001` | P1 | Tournament CPU realism | Broader non-Badugi pro-overlay tournament audit remains too nit/passive. | `MGX_TOURNAMENT_AI_FEEDBACK_AUDIT.md`; `meaningful-decision-density.json`; `badugi-cpu-pressure-comparison.json`. | Tournament feedback quality can collapse if deployed path is nit/passive. | Keep schema proof separate; schedule broader pro-overlay tuning after source attribution. |
| `MEANINGFUL-DECISION-001` | P1 | Tournament decision density | Heuristic path has good density, but pro-overlay tournament audit collapses to low meaningful decisions; live natural telemetry is insufficient. | Tournament AI audit docs; meaningful-decision reports; Badugi value-bet live observation. | Friend alpha tournament quality depends on deployed path not collapsing. | Measure deployed session decision density by sessionId before GO. |
