# Managed by scripts/deploy/mgx-prod-01.sh.
# Intentionally empty: defining a partial `types` map in nginx's http context
# replaces the inherited MIME map and can make HTML download as octet-stream.
# The site templates set application/wasm only for the static WASM location.
